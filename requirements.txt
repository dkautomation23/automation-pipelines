# Webhook service
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
# FastAPI needs this for the UploadFile endpoint; it is not a FastAPI dependency.
python-multipart>=0.0.9
python-dotenv>=1.0
requests>=2.32

# Google Sheets sink (optional - only needed when SHEETS_ENABLED=true)
gspread>=6.0
google-auth>=2.30

# Document extraction
pdfplumber>=0.11

# Development / tests
pytest>=8.0
httpx>=0.27
